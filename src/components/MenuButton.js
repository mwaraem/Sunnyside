import menu from "../images/icon-hamburger.svg"

import React from 'react'

const MenuButton = () => {
    return (
        <>
            <button>
                <img src={menu} alt="" />
            </button>
        </>
    )
}

export default MenuButton